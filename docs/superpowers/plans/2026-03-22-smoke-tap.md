# Smoke Tap Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 기존 5탭 Expo boilerplate를 흡연 횟수 기록 앱(Smoke Tap)으로 변환한다.

**Architecture:** Zustand + AsyncStorage로 탭 기록을 영속 저장하고, 커스텀 React Native View로 7일 막대 차트를 그린다. 외부 DB·차트 라이브러리 없이 기존 설치된 패키지만 사용한다.

**Tech Stack:** Expo Router, NativeWind v4, Zustand + AsyncStorage persist, expo-localization (Intl.DateTimeFormat), TypeScript strict

---

## 파일 맵

| 액션 | 경로 | 역할 |
|------|------|------|
| 삭제 | `app/(tabs)/list.tsx` | 불필요 boilerplate |
| 삭제 | `app/(tabs)/community.tsx` | 불필요 boilerplate |
| 삭제 | `app/(tabs)/history.tsx` | 불필요 boilerplate |
| 삭제 | `app/(tabs)/profile.tsx` | 불필요 boilerplate |
| 삭제 | `hooks/` (4개 파일) | 불필요 hooks |
| 삭제 | `services/mockApi.ts` | 불필요 mock |
| 삭제 | `store/useAppStore.ts` `useUserStore.ts` `useNotificationStore.ts` | 불필요 stores |
| 삭제 | `components/home/` (7개 파일) | 불필요 components |
| 삭제 | `constants/mockData.ts` `typography.ts` `spacing.ts` | 불필요 constants |
| 삭제 | `types/user.ts` `service.ts` `notification.ts` `content.ts` `navigation.ts` | 불필요 types |
| 신규 | `types/tap.ts` | TapRecord, DailyStat 타입 |
| 신규 | `store/useTapStore.ts` | 탭 기록 + computed (오늘 횟수·마지막 시각·일별 통계) |
| 신규 | `i18n/locales/ko.json` | 한국어 문자열 |
| 신규 | `i18n/index.ts` | t() 번역 함수 (dot-notation, {{param}} 치환) |
| 신규 | `components/stats/BarChart.tsx` | 7일 커스텀 막대 차트 |
| 수정 | `app/(tabs)/_layout.tsx` | 5탭 → 3탭 (메인/통계/설정) |
| 수정 | `app/_layout.tsx` | QueryClientProvider 제거 |
| 수정 | `app/(tabs)/index.tsx` | 메인 화면 전면 교체 |
| 신규 | `app/(tabs)/stats.tsx` | 통계 화면 |
| 신규 | `app/(tabs)/settings.tsx` | 설정 화면 (앱 버전) |

---

## Task 1: 불필요 파일 정리

**Files:**
- Delete: `app/(tabs)/list.tsx`, `community.tsx`, `history.tsx`, `profile.tsx`
- Delete: `hooks/useHomeData.ts`, `hooks/useServices.ts`, `hooks/useNotifications.ts`, `hooks/useSegmentContent.ts`
- Delete: `services/mockApi.ts`
- Delete: `store/useAppStore.ts`, `store/useUserStore.ts`, `store/useNotificationStore.ts`
- Delete: `components/home/` (전체 디렉토리)
- Delete: `constants/mockData.ts`, `constants/typography.ts`, `constants/spacing.ts`
- Delete: `types/user.ts`, `types/service.ts`, `types/notification.ts`, `types/content.ts`, `types/navigation.ts`

- [ ] **Step 1: 불필요 탭 화면 삭제**

```bash
rm app/\(tabs\)/list.tsx app/\(tabs\)/community.tsx app/\(tabs\)/history.tsx app/\(tabs\)/profile.tsx
```

- [ ] **Step 2: hooks, services, store 삭제**

```bash
rm -rf hooks/ services/
rm store/useAppStore.ts store/useUserStore.ts store/useNotificationStore.ts
```

- [ ] **Step 3: components/home, constants, types 삭제**

```bash
rm -rf components/home/
rm constants/mockData.ts constants/typography.ts constants/spacing.ts
rm types/user.ts types/service.ts types/notification.ts types/content.ts types/navigation.ts
```

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "chore: remove boilerplate files not needed for smoke-tap"
```

---

## Task 2: 타입 정의

**Files:**
- Create: `types/tap.ts`

- [ ] **Step 1: `types/tap.ts` 작성**

```typescript
export type TapRecord = {
  id: string;        // timestamp-based unique ID
  timestamp: number; // Unix ms (device local time)
};

export type DailyStat = {
  date: string;  // 'YYYY-MM-DD' (device local date)
  count: number;
};
```

- [ ] **Step 2: 타입 컴파일 확인**

```bash
npx tsc --noEmit
```

Expected: 에러 없음 (또는 아직 삭제된 import 관련 에러만 — 다음 태스크에서 해결됨)

- [ ] **Step 3: Commit**

```bash
git add types/tap.ts
git commit -m "feat: add TapRecord and DailyStat types"
```

---

## Task 3: useTapStore

**Files:**
- Create: `store/useTapStore.ts`

- [ ] **Step 1: `store/useTapStore.ts` 작성**

```typescript
import { create } from 'zustand';
import { persist, createJSONStorage } from 'zustand/middleware';
import AsyncStorage from '@react-native-async-storage/async-storage';
import type { TapRecord, DailyStat } from '../types/tap';

type TapState = {
  records: TapRecord[];
  addTap: () => void;
  getTodayCount: () => number;
  getLastTapTime: () => number | null;
  getDailyStats: (days: number) => DailyStat[];
};

// Returns 'YYYY-MM-DD' in device local timezone
function toLocalDateString(ts: number): string {
  const d = new Date(ts);
  const y = d.getFullYear();
  const m = String(d.getMonth() + 1).padStart(2, '0');
  const day = String(d.getDate()).padStart(2, '0');
  return `${y}-${m}-${day}`;
}

export const useTapStore = create<TapState>()(
  persist(
    (set, get) => ({
      records: [],

      addTap: () =>
        set((state) => ({
          records: [
            ...state.records,
            { id: String(Date.now()), timestamp: Date.now() },
          ],
        })),

      getTodayCount: () => {
        const today = toLocalDateString(Date.now());
        return get().records.filter(
          (r) => toLocalDateString(r.timestamp) === today
        ).length;
      },

      getLastTapTime: () => {
        const { records } = get();
        return records.length ? records[records.length - 1].timestamp : null;
      },

      getDailyStats: (days: number): DailyStat[] => {
        const { records } = get();
        const stats: DailyStat[] = [];
        for (let i = days - 1; i >= 0; i--) {
          const d = new Date();
          d.setDate(d.getDate() - i);
          d.setHours(0, 0, 0, 0);
          const dateStr = toLocalDateString(d.getTime());
          const count = records.filter(
            (r) => toLocalDateString(r.timestamp) === dateStr
          ).length;
          stats.push({ date: dateStr, count });
        }
        return stats;
      },
    }),
    {
      name: 'tap-storage',
      storage: createJSONStorage(() => AsyncStorage),
    }
  )
);
```

- [ ] **Step 2: 타입 컴파일 확인**

```bash
npx tsc --noEmit
```

Expected: 에러 없음

- [ ] **Step 3: Commit**

```bash
git add store/useTapStore.ts
git commit -m "feat: add useTapStore with AsyncStorage persistence"
```

---

## Task 4: i18n

**Files:**
- Create: `i18n/locales/ko.json`
- Create: `i18n/index.ts`

> Note: `expo-localization`은 `expo` 패키지에 포함되어 있어 별도 설치 불필요.

- [ ] **Step 1: `i18n/locales/ko.json` 작성**

```json
{
  "tabs": {
    "main": "메인",
    "stats": "통계",
    "settings": "설정"
  },
  "main": {
    "todayCount": "오늘 {{count}}회",
    "lastTap": "마지막: {{time}}",
    "noTapYet": "아직 기록 없음"
  },
  "stats": {
    "title": "지난 7일",
    "noData": "데이터 없음"
  },
  "settings": {
    "appVersion": "앱 버전"
  }
}
```

- [ ] **Step 2: `i18n/index.ts` 작성**

```typescript
import * as Localization from 'expo-localization';
import ko from './locales/ko.json';

type LocaleDict = typeof ko;

const locales: Record<string, LocaleDict> = { ko };

function getDict(): LocaleDict {
  const lang = Localization.getLocales()[0]?.languageCode ?? 'ko';
  return locales[lang] ?? locales['ko'];
}

function getNestedValue(
  obj: Record<string, unknown>,
  keys: string[]
): string {
  let current: unknown = obj;
  for (const key of keys) {
    if (typeof current !== 'object' || current === null) return '';
    current = (current as Record<string, unknown>)[key];
  }
  return typeof current === 'string' ? current : '';
}

export function t(
  key: string,
  params?: Record<string, string | number>
): string {
  const dict = getDict();
  let str = getNestedValue(
    dict as unknown as Record<string, unknown>,
    key.split('.')
  );
  if (!str) return key;
  if (params) {
    Object.entries(params).forEach(([k, v]) => {
      str = str.replace(`{{${k}}}`, String(v));
    });
  }
  return str;
}
```

- [ ] **Step 3: 타입 컴파일 확인**

```bash
npx tsc --noEmit
```

Expected: 에러 없음

- [ ] **Step 4: Commit**

```bash
git add i18n/
git commit -m "feat: add i18n with Korean locale and t() helper"
```

---

## Task 5: BarChart 컴포넌트

**Files:**
- Create: `components/stats/BarChart.tsx`

- [ ] **Step 1: `components/stats/BarChart.tsx` 작성**

```typescript
import React from 'react';
import { View, Text } from 'react-native';
import type { DailyStat } from '../../types/tap';
import { t } from '../../i18n';

type Props = {
  data: DailyStat[];
};

const BAR_MAX_HEIGHT = 120;

export default function BarChart({ data }: Props) {
  // Always render bars for all days; maxCount guard prevents division by zero
  const maxCount = Math.max(...data.map((d) => d.count), 1);

  return (
    <View className="flex-row items-end justify-around px-4 pb-4">
      {data.map((item) => {
        const barHeight = (item.count / maxCount) * BAR_MAX_HEIGHT;
        // Format: M/D (no leading zero)
        const [, month, day] = item.date.split('-');
        const label = `${parseInt(month)}/${parseInt(day)}`;

        return (
          <View key={item.date} className="items-center">
            <Text className="text-xs text-gray-600 mb-1">
              {item.count > 0 ? item.count : ''}
            </Text>
            <View
              style={{ height: Math.max(barHeight, 2), width: 28 }}
              className="bg-blue-500 rounded-t-md"
            />
            <Text className="text-xs text-gray-500 mt-1">{label}</Text>
          </View>
        );
      })}
    </View>
  );
}
```

- [ ] **Step 2: 타입 컴파일 확인**

```bash
npx tsc --noEmit
```

Expected: 에러 없음

- [ ] **Step 3: Commit**

```bash
git add components/stats/BarChart.tsx
git commit -m "feat: add custom BarChart component for 7-day stats"
```

---

## Task 6: app/_layout.tsx — QueryClient 제거

**Files:**
- Modify: `app/_layout.tsx`

현재 파일은 `QueryClientProvider`로 감싸져 있다. TanStack Query를 제거하고 단순한 Stack 라우터만 남긴다.

- [ ] **Step 1: `app/_layout.tsx` 교체**

```typescript
import '../global.css';
import { Stack } from 'expo-router';

export default function RootLayout() {
  return (
    <Stack>
      <Stack.Screen name="(tabs)" options={{ headerShown: false }} />
    </Stack>
  );
}
```

- [ ] **Step 2: 타입 컴파일 확인**

```bash
npx tsc --noEmit
```

Expected: 에러 없음

- [ ] **Step 3: Commit**

```bash
git add app/_layout.tsx
git commit -m "chore: remove QueryClientProvider from root layout"
```

---

## Task 7: 탭 레이아웃 — 5탭 → 3탭

**Files:**
- Modify: `app/(tabs)/_layout.tsx`

- [ ] **Step 1: `app/(tabs)/_layout.tsx` 교체**

```typescript
import React from 'react';
import { Tabs } from 'expo-router';
import { Ionicons } from '@expo/vector-icons';
import { t } from '../../i18n';

export default function TabLayout() {
  return (
    <Tabs
      screenOptions={{
        headerShown: false,
        tabBarActiveTintColor: '#007AFF',
        tabBarInactiveTintColor: '#8E8E93',
        tabBarStyle: {
          backgroundColor: '#FFFFFF',
          borderTopColor: '#C6C6C8',
        },
      }}
    >
      <Tabs.Screen
        name="index"
        options={{
          tabBarLabel: t('tabs.main'),
          tabBarIcon: ({ color, focused }) => (
            <Ionicons
              name={focused ? 'home' : 'home-outline'}
              size={24}
              color={color}
            />
          ),
        }}
      />
      <Tabs.Screen
        name="stats"
        options={{
          tabBarLabel: t('tabs.stats'),
          tabBarIcon: ({ color, focused }) => (
            <Ionicons
              name={focused ? 'bar-chart' : 'bar-chart-outline'}
              size={24}
              color={color}
            />
          ),
        }}
      />
      <Tabs.Screen
        name="settings"
        options={{
          tabBarLabel: t('tabs.settings'),
          tabBarIcon: ({ color, focused }) => (
            <Ionicons
              name={focused ? 'settings' : 'settings-outline'}
              size={24}
              color={color}
            />
          ),
        }}
      />
    </Tabs>
  );
}
```

- [ ] **Step 2: 타입 컴파일 확인**

```bash
npx tsc --noEmit
```

Expected: 에러 없음

- [ ] **Step 3: Commit**

```bash
git add "app/(tabs)/_layout.tsx"
git commit -m "feat: replace 5-tab layout with 3-tab smoke-tap layout"
```

---

## Task 8: 메인 화면

**Files:**
- Modify: `app/(tabs)/index.tsx`

- [ ] **Step 1: `app/(tabs)/index.tsx` 전면 교체**

```typescript
import React from 'react';
import { View, Text, TouchableOpacity, SafeAreaView } from 'react-native';
import { useTapStore } from '../../store/useTapStore';
import { t } from '../../i18n';

function formatTime(ts: number): string {
  return new Intl.DateTimeFormat(undefined, {
    hour: 'numeric',
    minute: '2-digit',
    hour12: true,
  }).format(new Date(ts));
}

export default function MainScreen() {
  const addTap = useTapStore((s) => s.addTap);
  const getTodayCount = useTapStore((s) => s.getTodayCount);
  const getLastTapTime = useTapStore((s) => s.getLastTapTime);

  const todayCount = getTodayCount();
  const lastTapTime = getLastTapTime();

  return (
    <SafeAreaView className="flex-1 bg-white">
      <View className="flex-1 items-center justify-center gap-8">
        <Text className="text-4xl font-bold text-gray-800">
          {t('main.todayCount', { count: todayCount })}
        </Text>

        <TouchableOpacity
          onPress={addTap}
          className="w-20 h-20 rounded-full bg-blue-500 items-center justify-center shadow-lg"
          activeOpacity={0.7}
        >
          <Text className="text-white text-5xl font-light leading-none">+</Text>
        </TouchableOpacity>

        <Text className="text-base text-gray-500">
          {lastTapTime
            ? t('main.lastTap', { time: formatTime(lastTapTime) })
            : t('main.noTapYet')}
        </Text>
      </View>
    </SafeAreaView>
  );
}
```

- [ ] **Step 2: 타입 컴파일 확인**

```bash
npx tsc --noEmit
```

Expected: 에러 없음

- [ ] **Step 3: iOS 시뮬레이터에서 수동 확인**

```bash
npx expo start --ios
```

확인 항목:
- 메인 탭 로딩 — "오늘 0회" + "아직 기록 없음" 표시
- + 버튼 탭 시 "오늘 1회", "오늘 2회" ... 즉시 증가
- 마지막 탭 시각 표시 (기기 로케일 포맷)

- [ ] **Step 4: Commit**

```bash
git add "app/(tabs)/index.tsx"
git commit -m "feat: implement main tap screen"
```

---

## Task 9: 통계 화면

**Files:**
- Create: `app/(tabs)/stats.tsx`

- [ ] **Step 1: `app/(tabs)/stats.tsx` 작성**

```typescript
import React from 'react';
import { View, Text, SafeAreaView } from 'react-native';
import { useTapStore } from '../../store/useTapStore';
import { t } from '../../i18n';
import BarChart from '../../components/stats/BarChart';

export default function StatsScreen() {
  const getDailyStats = useTapStore((s) => s.getDailyStats);
  const data = getDailyStats(7);

  return (
    <SafeAreaView className="flex-1 bg-white">
      <View className="px-4 pt-8 pb-4">
        <Text className="text-2xl font-bold text-gray-800">
          {t('stats.title')}
        </Text>
      </View>
      <View className="flex-1 justify-center">
        <BarChart data={data} />
      </View>
    </SafeAreaView>
  );
}
```

- [ ] **Step 2: 타입 컴파일 확인**

```bash
npx tsc --noEmit
```

Expected: 에러 없음

- [ ] **Step 3: iOS 시뮬레이터에서 수동 확인**

확인 항목:
- 통계 탭 — 7개 막대 표시 (데이터 없으면 "데이터 없음")
- 메인에서 탭 후 통계 탭 이동 시 오늘 막대 높이 증가 확인

- [ ] **Step 4: Commit**

```bash
git add "app/(tabs)/stats.tsx"
git commit -m "feat: implement stats screen with 7-day bar chart"
```

---

## Task 10: 설정 화면

**Files:**
- Create: `app/(tabs)/settings.tsx`

- [ ] **Step 1: `app/(tabs)/settings.tsx` 작성**

```typescript
import React from 'react';
import { View, Text, SafeAreaView } from 'react-native';
import Constants from 'expo-constants';
import { t } from '../../i18n';

export default function SettingsScreen() {
  const version = Constants.expoConfig?.version ?? '—';

  return (
    <SafeAreaView className="flex-1 bg-white">
      <View className="px-4 pt-8">
        <Text className="text-2xl font-bold text-gray-800 mb-8">
          {t('tabs.settings')}
        </Text>
        <View className="flex-row justify-between items-center py-4 border-b border-gray-100">
          <Text className="text-base text-gray-700">
            {t('settings.appVersion')}
          </Text>
          <Text className="text-base text-gray-400">{version}</Text>
        </View>
      </View>
    </SafeAreaView>
  );
}
```

- [ ] **Step 2: 타입 컴파일 확인**

```bash
npx tsc --noEmit
```

Expected: 에러 없음

- [ ] **Step 3: iOS 시뮬레이터에서 수동 확인**

확인 항목:
- 설정 탭 — "앱 버전" 레이블 + `app.json`의 version 값 표시

- [ ] **Step 4: Commit**

```bash
git add "app/(tabs)/settings.tsx"
git commit -m "feat: implement settings screen with app version"
```

---

## Task 11: 최종 검증

- [ ] **Step 1: TypeScript 전체 검사**

```bash
npx tsc --noEmit
```

Expected: 에러 없음

- [ ] **Step 2: AsyncStorage 영속성 확인**

1. 메인 탭에서 + 버튼 수회 탭
2. 앱 종료 후 재시작
3. 오늘 횟수와 마지막 시각이 유지되는지 확인

- [ ] **Step 3: 통계 정확성 확인**

1. 오늘 N번 탭
2. 통계 탭에서 오늘 날짜 막대 높이가 N인지 확인

- [ ] **Step 4: 최종 커밋**

```bash
git add docs/
git commit -m "docs: add PRD, tasks, and implementation plan for smoke-tap"
```
